# Prompts

This directory stores prompt design notes and reusable prompt fragments.

The MVP compiler uses `templates/codex_task_prompt.j2` as the executable template. Files in this directory should stay human-readable and should not call external services.
