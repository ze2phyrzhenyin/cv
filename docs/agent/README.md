# Agent Notes

This directory is for agent-facing documentation that expands on `AGENTS.md`.

The MVP framework is dry-run only. It may compile instructions for future agent runs, but it must not call GitHub, deployment systems, MCP tools, the Codex API, or `codex exec`.
